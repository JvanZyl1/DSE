classdef Gear

    properties
        name = 'gear'
        area
        length
        radius
    end
    
    methods
        function obj = Beam(length, radius, thickness)
            obj.self.area = 2 * pi * thickness * radius;
            obj.length = 0.2;
            obj.radius = radius;
        end
    end
end

